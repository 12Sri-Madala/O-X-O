export type driver = {
  id: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  checkr: string;
  customerID: string;
};

export type owner = {
  id: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  accountId: string;
};
