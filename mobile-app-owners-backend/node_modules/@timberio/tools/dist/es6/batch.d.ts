import { ITimberLog } from "@timberio/types";
/**
 * batch the buffer coming in, process them and then resolve
 *
 * @param size - Number
 * @param flushTimeout - Number
 */
export default function makeBatch(size?: number, flushTimeout?: number): (fn: Function) => (log: ITimberLog) => Promise<ITimberLog>;
